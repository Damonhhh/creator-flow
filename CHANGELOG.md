# CreatorFlow 更新日志

这里记录每个正式版本对使用者可见的变化。开发过程中的零散提交不单独列出，只有通过公开发布审计并合入 `main` 的内容才会进入版本说明。

## [0.2.0] - 2026-08-24

### 补全发布收尾链路

- 发布文案验收现在绑定最终脚本和最终成片的 SHA-256，并同时核对评分卡、`latest-render.json` 与 `qa-stamp.json`。
- 脚本或成片发生变化后，旧评分卡会主动失效，防止修改成片后继续沿用旧结论。
- 收尾阶段补入标题候选、正文、首评、封面检查、人工画面复核、发布包与同步记录的完整门禁。
- 增加画面所有权和真人主讲混合素材规则，减少临时信息卡、空素材位和长时间静态画面混入最终成片。

### 环境与依赖

- 增加 Core、Agent Reach 素材、IndexTTS2 配音和 HyperFrames 组装四个环境辅助包。
- 增加 Windows 离线资源包说明，方便网络条件受限的使用者自行准备依赖。
- 依赖恢复继续遵守明确授权：Agent 先说明用途、来源、命令、修改范围和降级方案，得到同意后才下载或安装。

### 公开包可靠性

- 公开版封面文档只引用 `docs/cover-system.md`，不再暴露私有项目路径。
- 发布清单统一按规范化换行符计算文本 SHA-256，避免 Windows 的 CRLF/LF 转换造成误报。
- Core 发布审计通过：132 个清单文件、131 个 SHA-256、隐私扫描和 21 项发布测试全部通过。

### 升级提醒

历史项目如果缺少新的脚本或成片哈希字段，发布文案门禁会失败。请基于当前最终脚本和成片重新生成评分卡，不要手工复制旧哈希绕过检查。
