---
name: zimeiti-video-wrap-up
description: Complete CreatorFlow QA evidence, covers, publishing copy, publish package, and final sync.
---

# CreatorFlow adapter for Claude Code

Read `../../../.agents/skills/zimeiti-video-wrap-up/SKILL.md` completely and use it as the authoritative wrap-up workflow.

Work from the repository root. Resolve the canonical skill's `references/` from `.agents/skills/zimeiti-video-wrap-up/`, and resolve `docs/`, `scripts/`, `config/`, `examples/`, and `videos/` from the repository root.

Do not duplicate or override workflow rules in this adapter.
