---
name: zimeiti-video-workflow
description: Run CreatorFlow from topic selection through script, material planning, assembly, QA, and publish wrap-up.
---

# CreatorFlow adapter for Claude Code

Read `../../../.agents/skills/zimeiti-video-workflow/SKILL.md` completely and use it as the authoritative workflow.

Work from the repository root. Resolve the canonical skill's `references/` from `.agents/skills/zimeiti-video-workflow/`, and resolve `docs/`, `scripts/`, `config/`, `examples/`, and `videos/` from the repository root.

Do not duplicate or override workflow rules in this adapter.
